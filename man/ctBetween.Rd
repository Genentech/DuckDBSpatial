% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate-transform-graph.R
\name{ctBetween}
\alias{ctBetween}
\title{Transform points between two coordinate systems}
\usage{
ctBetween(graph, from, to, points)
}
\arguments{
\item{graph}{A \code{CTgraph} built with \code{systems} (so axis orders are
known).}

\item{from, to}{Coordinate-system names.}

\item{points}{A numeric matrix (rows = points) or a vector (one point).}
}
\value{
A numeric matrix of transformed points.
}
\description{
Resolves the transform with \code{\link{ctPath}} and applies it to a matrix
of points with \code{\link{ctApply}}, using the axis orders recorded for the
two coordinate systems in the graph.
}
\examples{
g <- ctGraph(list(list(input = "a", output = "b",
             transform = ctScale(c(2, 3)))),
             systems = list(a = c("y", "x"), b = c("y", "x")))
ctBetween(g, "a", "b", rbind(c(1, 1)))
}
