% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate-transforms.R
\name{coordinate-transforms}
\alias{coordinate-transforms}
\alias{ctIdentity}
\alias{ctScale}
\alias{ctTranslation}
\alias{ctRotation}
\alias{ctAffine}
\alias{ctSequence}
\alias{ctMapAxis}
\alias{ctByDimension}
\alias{ctBijection}
\title{Coordinate-transform constructors (OME-NGFF RFC-5)}
\usage{
ctIdentity()

ctScale(scale)

ctTranslation(translation)

ctRotation(rotation)

ctAffine(affine)

ctSequence(transformations)

ctMapAxis(map)

ctByDimension(parts)

ctBijection(forward, inverse)
}
\arguments{
\item{scale, translation}{Numeric per-axis vectors.}

\item{rotation, affine}{A rotation (square) or affine (\eqn{n_{out} \times
(n_{in}+1)}, RFC-5 non-homogeneous) matrix, as a matrix or list of rows.}

\item{transformations}{A list of \code{CoordinateTransform}s (applied first
to last) for \code{ctSequence}.}

\item{map}{Integer 0-based axis permutation: output axis \code{i} is taken
from input axis \code{map[i]}.}

\item{parts}{For \code{ctByDimension}, a list of
\code{list(transformation=, input_axes=, output_axes=)} (axes 0-based),
each sub-transform acting on its own axis subset.}

\item{forward, inverse}{\code{CoordinateTransform}s giving an explicit
forward/inverse pair for \code{ctBijection}.}
}
\value{
A \code{CoordinateTransform}.
}
\description{
Build the RFC-5 affine-family coordinate transforms. Each returns a
\code{CoordinateTransform} (a classed list mirroring the RFC-5 / SpatialData
on-disk dict), consumed by \code{\link{ctAffineMatrix}} /
\code{\link{ctApply}} and the coordinate-transform graph
(\code{\link{ctGraph}}). Transforms are positional: they act on the
coordinate vector in the coordinate system's axis order.
}
\examples{
ctScale(c(10, 20))
ctSequence(list(ctScale(c(10, 20)), ctTranslation(c(1, 1))))

}
