% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DuckDBDataFrame-spatial.R
\name{layerSubsetByBbox}
\alias{layerSubsetByBbox}
\title{Row indices of a layer inside a bounding box}
\usage{
layerSubsetByBbox(
  x,
  xmin,
  xmax,
  ymin,
  ymax,
  x_col = "x",
  y_col = "y",
  geom = "geometry"
)
}
\arguments{
\item{x}{A \code{DuckDBDataFrame} or in-memory tabular layer.}

\item{xmin, xmax, ymin, ymax}{Bounding box limits.}

\item{x_col, y_col}{Coordinate column names for point layers.}

\item{geom}{Geometry column name for polygon layers.}
}
\value{
Integer row indices.
}
\description{
Row indices of a layer inside a bounding box
}
\examples{
pts_path <- tempfile(fileext = ".csv")
on.exit(unlink(pts_path), add = TRUE)
write.csv(data.frame(x = c(1, 5, 10), y = c(1, 5, 10)), pts_path, row.names = FALSE)
pts <- DuckDBDataFrame(pts_path, datacols = c("x", "y"))
layerSubsetByBbox(pts, 0, 10, 0, 10, x_col = "x", y_col = "y")

}
